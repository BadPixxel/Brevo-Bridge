
{% if object is defined and object.htmlContent is not empty %}
    
    <iframe 
        id="sib-email-{{ object.id }}" src="{{ admin.generateObjectUrl('preview', object) }}"
        style="width: 100%; height: 800px;"
    >
    </iframe>
        
{% endif %}